<?xml version='1.0' encoding='UTF-8'?>
<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" version="2.0"><channel><title>Luliy</title><link>https://luliy.me</link><description>我将无限进步!</description><copyright>Luliy</copyright><docs>http://www.rssboard.org/rss-specification</docs><generator>python-feedgen</generator><image><url>https://avatars.githubusercontent.com/u/177055996?u=cb497e1edb10d059b285288c93ff32dab53af1c3&amp;v=4&amp;size=64</url><title>avatar</title><link>https://luliy.me</link></image><lastBuildDate>Wed, 10 Jun 2026 17:19:14 +0000</lastBuildDate><managingEditor>Luliy</managingEditor><ttl>60</ttl><webMaster>Luliy</webMaster><item><title>Luliy Blog 三件套完整说明书</title><link>https://luliy.me/post/Luliy%20Blog%20-san-jian-tao-wan-zheng-shuo-ming-shu.html</link><description># 🌸 Luliy Blog 三件套完整说明书

&gt; 本文档用中文详细解释 `config.json`、`enhance.css`、`enhance.js` 三个文件的每一处功能、可修改的参数，以及新手如何理解和动手改动。</description><guid isPermaLink="true">https://luliy.me/post/Luliy%20Blog%20-san-jian-tao-wan-zheng-shuo-ming-shu.html</guid><pubDate>Mon, 08 Jun 2026 15:28:11 +0000</pubDate></item><item><title>JS CSS 详解</title><link>https://luliy.me/post/JS%20CSS%20-xiang-jie.html</link><description># 📘 Luliy Blog v8 完整技术说明书

## 一、架构总览

```
Luliy Blog v8
├── 📁 js/
│   └── main.js          # 24个模块，按序加载
├── 📁 css/
│   └── enhance.css      # 27个区域，主题驱动
├── 📄 config.json       # 博客核心配置
├── 📄 theme.json        # 主题预设（4主题）
└── 📄 manifest.json     # PWA 配置
```

**设计哲学**：模块化加载 → 主题变量驱动 → 渐进增强

---

## 二、JS 模块详解

### `Module 00` — Welcome Splash 欢迎闪屏
```javascript
// 功能：页面首次加载时的品牌展示动画
// 触发：sessionStorage 控制，同会话只显示一次
```
| 属性 | 说明 |
|------|------|
| 触发条件 | `sessionStorage.getItem('visited') !== 'true'` |
| 动画序列 | fadeIn → 品牌文字 → progress填充 → fadeOut |
| 阻断性 | **阻塞后续模块加载**，动画完成后才执行 01-23 |

**修改方式**：
```javascript
// 修改动画时长（默认 2500ms）
const SPLASH_DURATION = 3000;  // 延长到3秒

// 修改显示频率：每次刷新都显示
// 删除或注释掉 sessionStorage 相关代码

// 自定义品牌文字
const BRAND_TEXT = 'Your Name';  // 替换 'Luliy'
```

---

### `Module 01` — localStorage Init 本地存储初始化
```javascript
// 功能：建立持久化配置层，所有用户偏好保存于此
// 数据结构：{ theme, fontSize, reducedMotion, soundEnabled, ... }
```
| 存储键 | 用途 | 默认值 |
|--------|------|--------|
| `luliy_theme` | 当前主题ID | `'sakura'` |
| `luliy_font` | 字体缩放级别 | `'md'` |
| `luliy_motion` | 减少动画偏好 | `false` |
| `luliy_sound` | 音效开关 | `true` |
| `luliy_visited` | 访问标记（splash用）| — |

**修改方式**：
```javascript
// 新增自定义配置项
localStorage.setItem('luliy_customGrid', '3');  // 新增：网格列数

// 读取时
const gridCols = localStorage.getItem('luliy_customGrid') || '2';
```

---

### `Module 02` — Progress Bar 进度条
```javascript
// 功能：顶部阅读进度指示器 + 页面加载进度
// 双模式：loading（资源加载）| reading（滚动深度）
```
| 模式 | 触发条件 | 颜色来源 |
|------|----------|----------|
| Loading | `document.readyState !== 'complete'` | `--accent` |
| Reading | 滚动事件 | 当前主题主色 |

**修改方式**：
```javascript
// 改为底部显示
const progressBar = document.createElement('div');
progressBar.style.top = 'auto';
progressBar.style.bottom = '0';  // 底部进度条

// 添加缓冲动画效果
progressBar.style.transition = 'width 0.1s cubic-bezier(0.25, 0.46, 0.45, 0.94)';
```

---

### `Module 03` — Dynamic Title 动态标题
```javascript
// 功能：标签页失焦时显示自定义消息，吸引用户回流
```
| 状态 | 标题变化 |
|------|----------|
| 页面可见 | 原始标题 `'文章标题 | Luliy Blog'` |
| 页面隐藏 | `'(◍•ᴗ•◍) 快回来~' + 原始标题` |

**修改方式**：
```javascript
// 自定义失焦消息数组（随机轮换）
const AWAY_MESSAGES = [
    '✨ 发现好文不看完吗？',
    '🌸 樱花飘落中，等你回来',
    '📚 知识正在冷却...',
    '🎵 音乐暂停了'
];

// 根据页面类型定制
const isPostPage = location.pathname.includes('/post/');
const message = isPostPage ? '📖 读一半呢，别走~' : AWAY_MESSAGES[random];
```

---

### `Module 04` — Uptime Counter 运行时间计数器
```javascript
// 功能：显示博客自创建以来的运行时长
// 格式：X年X月X日 X时X分X秒 | 或精简版
```
| 显示位置 | 默认 | 可选 |
|----------|------|------|
| Footer | 完整格式 | 精简图标版 |
| 英雄区 | 无 | 徽章式 |

**修改方式**：
```javascript
// 修改起始时间（博客创建日期）
const BIRTH_DATE = new Date('2020-03-15T00:00:00+08:00');  // 你的建站日

// 改为显示'本站已陪伴你'（访客视角）
const visitorFirstVisit = localStorage.getItem('first_visit');
const displayTime = visitorFirstVisit 
    ? timeSince(new Date(visitorFirstVisit))  // 个人视角
    : timeSince(BIRTH_DATE);                  // 站点视角
```

---

### `Module 05` — Dark-mode Ripple 深色模式涟漪
```javascript
// 功能：主题切换时的视觉过渡效果
// 原理：点击位置创建 radial-gradient 扩散动画
```
| 参数 | 说明 |
|------|------|
| 动画时长 | 600ms |
| 缓动函数 | `cubic-bezier(0.4, 0, 0.2, 1)` |
| 覆盖范围 | 全视口 |

**修改方式**：
```javascript
// 改为从中心扩散（而非点击位置）
const centerX = window.innerWidth / 2;
const centerY = window.innerHeight / 2;

// 添加声音反馈（配合 Module 07）
ripple.addEventListener('animationstart', () =&gt; {
    playSound('whoosh', 0.3);
});
```

---

### `Module 06` — Static Background 静态背景
```javascript
// 功能：主题相关的背景图/渐变（v8移除粒子性能开销）
// 替代方案：CSS 渐变 + 可选的轻量 SVG 纹理
```
| 主题 | 背景类型 |
|------|----------|
| `sakura` | 粉色渐变 + 花瓣纹理 SVG |
| `ocean` | 深蓝渐变 + 波浪 SVG |
| `forest` | 绿色渐变 + 叶脉纹理 |
| `stardust` | 深色渐变 + 星点 CSS |

**修改方式**：
```css
/* 添加自定义纹理 */
[data-theme='custom'] {
    --bg-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    --bg-texture: url('data:image/svg+xml,%3Csvg width='60' height='60'...'); /* 内联 SVG */
}
```

---

### `Module 07` — Web Audio SFX 音效系统
```javascript
// 功能：全站交互音效，基于 Web Audio API（非 HTML5 Audio）
// 特点：零依赖、低延迟、可合成多种音色
```
| 音效ID | 触发场景 | 音色类型 |
|--------|----------|----------|
| `click` | 按钮点击 | 短促正弦波 |
| `hover` | 卡片悬停 | 轻柔八度 |
| `switch` | 主题切换 | 滑音效果 |
| `success` | 操作成功 | 和弦上升 |
| `pop` | 菜单展开 | 气泡破裂 |
| `type` | 打字机（可选）| 机械键盘 |

**修改方式**：
```javascript
// 添加新音效：页面滚动到特定区域的反馈
function playScrollZoneSound(zoneName) {
    const frequencies = {
        'hero': 523.25,      // C5
        'content': 659.25,   // E5
        'footer': 783.99     // G5
    };
    playTone(frequencies[zoneName], 'sine', 0.15, 0.1);
}

// 全局静音检测（系统偏好）
if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    audioContext.suspend();  // 尊重用户系统设置
}
```

---

### `Module 08` — Click Sparks 点击火花
```javascript
// 功能：鼠标点击位置生成粒子爆发效果
// 与 Module 16 樱花的区别：火花是瞬时爆发，樱花是持续环境
```
| 参数 | 默认值 | 说明 |
|------|--------|------|
| 粒子数 | 8-12 | 随机 |
| 颜色 | 当前主题 accent | 动态继承 |
| 生命周期 | 600ms | CSS 动画 |
| 物理 | 无重力 | 纯扩散 |

**修改方式**：
```javascript
// 添加重力效果（抛物线）
spark.style.setProperty('--gravity', `${Math.random() * 50 + 20}px`);

// CSS 配合
@keyframes spark-fall {
    0% { transform: translate(0, 0) scale(1); }
    100% { transform: translate(var(--tx), calc(var(--ty) + var(--gravity))) scale(0); }
}

// 右键特殊效果（爱心形状）
document.addEventListener('contextmenu', (e) =&gt; {
    createHeartBurst(e.clientX, e.clientY);
    e.preventDefault();
});
```

---

### `Module 09` — Hero Cluster 英雄区集群
```javascript
// 功能：首页顶部核心信息展示区
// 组成：头像(avatar) + 站点名(name) + 实时时钟(clock)
```
```
布局结构：
┌─────────────────────────┐
│    ┌─────┐              │
│    │ 😊  │  Luliy       │
│    │avatar│  全栈开发者   │
│    └─────┘  ──── 14:32  │
│         [动态时钟]        │
└─────────────────────────┘
```

**修改方式**：
```javascript
// 时钟改为多时区显示
const clocks = [
    { zone: 'Asia/Shanghai', label: '北京' },
    { zone: 'America/New_York', label: '纽约' },
    { zone: 'Europe/London', label: '伦敦' }
];

// 头像添加状态指示（在线/忙碌/离线）
const statusIndicator = document.createElement('div');
statusIndicator.className = `status ${getCurrentStatus()}`; 
// CSS: .status.online { border-color: #00c853; }
```

---

### `Module 10` — Hero Banner 首页滚动折叠横幅
```javascript
// 功能：滚动时英雄区的视差+折叠效果
// 技术：scroll-driven animation (CSS) + JS fallback
```
| 滚动位置 | 效果 |
|----------|------|
| 0-100px | 正常显示，轻微视差 |
| 100-300px | 高度压缩，元素淡出 |
| &gt;300px | 完全折叠为迷你导航栏 |

**修改方式**：
```javascript
// 改为横向滚动触发（适用于横向布局网站）
const scrollProgress = container.scrollLeft / (container.scrollWidth - container.clientWidth);

// 添加毛玻璃强度随滚动变化
const blurAmount = Math.min(scrollY / 10, 20);
hero.style.backdropFilter = `blur(${blurAmount}px)`;
```

---

### `Module 11` — Tag Page Search Toolbar 标签页搜索工具栏
```javascript
// 功能：标签聚合页面的高级筛选
// 特性：实时搜索、多标签交集筛选、排序切换
```
| 功能 | 交互 |
|------|------|
| 搜索框 | 输入即时过滤，debounce 150ms |
| 标签云 | 点击添加/移除筛选条件 |
| 排序 | 时间/热度/字母 |
| 视图 | 网格/列表切换 |

**修改方式**：
```javascript
// 添加正则搜索支持
const isRegex = searchTerm.startsWith('/');
const pattern = isRegex ? new RegExp(searchTerm.slice(1, -1), 'i') : null;

// 添加搜索历史
const searchHistory = JSON.parse(localStorage.getItem('search_history') || '[]');
// 显示最近5条，点击快速填充

// 高级筛选：日期范围
const dateRange = {
    from: new Date('2024-01-01'),
    to: new Date()
};
```

---

### `Module 12` — Image Lightbox 图片灯箱
```javascript
// 功能：文章内图片点击放大查看
// 特性：手势支持、键盘导航、EXIF 信息展示
```
| 操作 | 功能 |
|------|------|
| 单击 | 打开灯箱 |
| 滚轮 | 缩放 |
| 拖拽 | 平移（放大后）|
| ←/→ | 切换同组图片 |
| ESC | 关闭 |

**修改方式**：
```javascript
// 添加图片对比模式（before/after）
lightbox.enableCompare = (img1, img2) =&gt; {
    return new CompareSlider(img1, img2);
};

// 添加下载原图按钮
const downloadBtn = document.createElement('a');
downloadBtn.href = originalUrl;
downloadBtn.download = filename;
// 需要后端支持原图访问

// 添加图片 OCR（调用 Tesseract.js）
const ocrBtn = document.createElement('button');
ocrBtn.onclick = async () =&gt; {
    const result = await Tesseract.recognize(imgElement);
    showTextOverlay(result.data.text);
};
```

---

### `Module 13` — Floating Toolbar + Unified Sink 浮动工具栏
```javascript
// 功能：全局可访问的快捷操作中心
// 4主题：每个主题有独立的工具栏配色和图标风格
```
```
工具栏组成：
┌────────────────────────────────┐
│ [🎨主题] [🔤字体] [🔊声音] [⚙️] │  ← 常驻
│         ▼ 展开更多              │
│ [📑目录] [🔖收藏] [💬评论] [↑] │
└────────────────────────────────┘
         ↓
    Unified Sink（统一收纳区）
    所有展开面板在此层叠管理
```

**修改方式**：
```javascript
// 添加自定义快捷工具
const customTools = [
    {
        id: 'translate',
        icon: '🌐',
        action: () =&gt; toggleTranslateOverlay(),
        shortcut: 'KeyT'
    },
    {
        id: 'reader',
        icon: '📖',
        action: () =&gt; enterReaderMode(),
        shortcut: 'KeyR'
    }
];

// 工具栏位置记忆
const savedPosition = localStorage.getItem('toolbar_pos');
toolbar.style.left = savedPosition?.x || 'auto';
toolbar.style.right = '20px';  // 默认右侧
```

---

### `Module 14` — Home Card Rebuild 首页卡片重构
```javascript
// 功能：文章列表的卡片式展示，v8 全新设计
// 特性：悬停3D倾斜、图片懒加载、阅读时间估算
```
| 卡片元素 | 说明 |
|----------|------|
| 封面图 | aspect-ratio 16/10，懒加载 + 模糊占位 |
| 分类标签 | 左上角绝对定位 |
| 标题 | 2行截断，悬停展开 |
| 摘要 | 3行截断 |
| 元信息 | 日期 · 阅读时间 · 点赞数 |
| 悬停效果 | 3D tilt + 阴影扩散 + 图片缩放 |

**修改方式**：
```javascript
// 添加阅读进度环（SVG）
const progressRing = `
&lt;svg class='progress-ring' viewBox='0 0 36 36'&gt;
    &lt;path d='M18 2.0845...' 
          stroke-dasharray='${readPercent}, 100'/&gt;
&lt;/svg&gt;`;

//  masonry 瀑布流布局
const masonry = new Masonry(grid, {
    itemSelector: '.card',
    columnWidth: '.card-sizer',
    gutter: 24
});

// 无限滚动替代分页
const observer = new IntersectionObserver((entries) =&gt; {
    entries[0].isIntersecting &amp;&amp; loadMorePosts();
});
observer.observe(sentinel);
```

---

### `Module 15` — macOS Code Block Strip macOS 风格代码块
```javascript
// 功能：仿 macOS Terminal 的代码展示
// 组成：三色圆点 + 标题栏 + 行号 + 语法高亮 + 复制按钮
```
```
┌─────────────────────────────┐  ← wrapper='code-window'
│ ● ● ●  filename.js          │  ← title bar (可选)
├─────────────────────────────┤
│ 1  │ const hello = 'world'; │  ← line numbers + code
│ 2  │ console.log(hello);    │
│ 3  │                        │
└─────────────────────────────┘
      [📋]  ← 悬浮复制按钮
```

**修改方式**：
```javascript
// 添加语言图标
const langIcons = {
    'javascript': '&lt;svg&gt;...&lt;/svg&gt;',
    'python': '&lt;svg&gt;...&lt;/svg&gt;',
    'rust': '&lt;svg&gt;...&lt;/svg&gt;'
};

// 添加代码执行（WebContainer 或 Pyodide）
const runButton = document.createElement('button');
runButton.onclick = async () =&gt; {
    const result = await webcontainer.run(codeBlock.textContent);
    showOutput(result);
};

// 添加 diff 高亮
const diffColors = {
    '+': 'var(--diff-add)',
    '-': 'var(--diff-del)',
    '@': 'var(--diff-meta)'
};
```

---

### `Module 16` — Sakura Petals 樱花花瓣
```javascript
// 功能：全屏 Canvas 花瓣飘落动画（v8 性能优化版）
// 与 v7 区别：使用 OffscreenCanvas + requestAnimationFrame 节流
```
| 参数 | 说明 |
|------|------|
| 最大花瓣数 | 50（移动端 25）|
| 物理模拟 | 风力 + 重力 + 旋转 |
| 颜色 | 主题关联（sakura=粉，ocean=蓝泡，forest=绿叶，stardust=星尘）|

**修改方式**：
```javascript
// 根据季节自动切换
const month = new Date().getMonth();
const seasonalEffects = {
    2: 'sakura',    // 3月春
    5: 'rain',      // 6月梅雨
    8: 'maple',     // 9月秋
    11: 'snow'      // 12月冬
};
setEffect(seasonalEffects[month] || 'none');

// 鼠标交互：花瓣避让
canvas.addEventListener('mousemove', (e) =&gt; {
    petals.forEach(petal =&gt; {
        const dx = petal.x - e.clientX;
        const dy = petal.y - e.clientY;
        const dist = Math.sqrt(dx*dx + dy*dy);
        if (dist &lt; 100) {
            petal.vx += dx * 0.01;
            petal.vy += dy * 0.01;
        }
    });
});
```

---

### `Module 17` — ArticleTOC Scroll-Spy + Back-to-Top 文章目录
```javascript
// 功能：文章页右侧/左侧浮动目录 + 滚动监听高亮
// 双组件：TOC 导航 + 返回顶部按钮（智能显隐）
```
| TOC 特性 | 说明 |
|----------|------|
| 生成源 | H2-H4 标题层级 |
| 滚动监听 | IntersectionObserver，rootMargin 优化 |
| 点击滚动 | smooth scroll，URL hash 更新 |
| 折叠展开 | 层级折叠，当前章节自动展开 |

| Back-to-Top | 说明 |
|-------------|------|
| 显示阈值 | 滚动超过 500px |
| 进度环 | SVG 圆周表示阅读进度 |
| 点击行为 | 平滑回顶 + 可选确认 |

**修改方式**：
```javascript
// TOC 添加阅读时间估算
const readingTime = Math.ceil(textLength / 400); // 400字/分钟
tocHeader.innerHTML = `目录 · 约 ${readingTime} 分钟`;

// Back-to-Top 添加章节快速跳转
const quickJumps = document.createElement('div');
quickJumps.innerHTML = `
    &lt;button data-target='prev'&gt;↑ 上一章&lt;/button&gt;
    &lt;button data-target='next'&gt;↓ 下一章&lt;/button&gt;
`;

// 滚动方向感知：向下滚动隐藏 TOC，向上显示
let lastScrollY = 0;
window.addEventListener('scroll', () =&gt; {
    const direction = window.scrollY &gt; lastScrollY ? 'down' : 'up';
    toc.classList.toggle('hidden', direction === 'down' &amp;&amp; scrollY &gt; 1000);
    lastScrollY = window.scrollY;
});
```

---

### `Module 18` — Mobile Nav Hamburger + Dropdown 移动端导航
```javascript
// 功能：小屏设备的响应式导航
// 特性：手势滑动打开、焦点陷阱、ARIA 无障碍
```
| 断点 | 行为 |
|------|------|
| &gt; 1024px | 水平导航栏，无汉堡 |
| 768-1024px | 简化导航，可选汉堡 |
| &lt; 768px | 全屏抽屉导航 |

**修改方式**：
```javascript
// 添加底部 Tab Bar（移动端 App 风格）
const tabBar = document.createElement('nav');
tabBar.className = 'mobile-tab-bar';
tabBar.innerHTML = `
    &lt;a href='/' ${isActive('/')}&gt;🏠 首页&lt;/a&gt;
    &lt;a href='/tags' ${isActive('/tags')}&gt;🏷️ 标签&lt;/a&gt;
    &lt;a href='/search' class='center-btn'&gt;🔍&lt;/a&gt;
    &lt;a href='/archive' ${isActive('/archive')}&gt;📚 归档&lt;/a&gt;
    &lt;a href='/about' ${isActive('/about')}&gt;👤 我&lt;/a&gt;
`;

// 手势：边缘右滑打开
let touchStartX = 0;
document.addEventListener('touchstart', e =&gt; touchStartX = e.touches[0].clientX);
document.addEventListener('touchend', e =&gt; {
    if (e.changedTouches[0].clientX - touchStartX &gt; 100 &amp;&amp; touchStartX &lt; 30) {
        openDrawer();
    }
});
```

---

### `Module 19` — Search Overlay 搜索遮罩层
```javascript
// 功能：全站内容搜索，覆盖式 UI
// 特性：快捷键唤起、本地索引、搜索结果高亮
```
| 快捷键 | 功能 |
|--------|------|
| `Cmd/Ctrl + K` | 打开搜索 |
| `ESC` | 关闭 |
| `↑/↓` | 结果导航 |
| `Enter` | 打开选中 |
| `Tab` | 切换搜索范围 |

**修改方式**：
```javascript
// 接入 Algolia 或 Meilisearch 实现全文搜索
const searchClient = algoliasearch('APP_ID', 'API_KEY');
const index = searchClient.initIndex('posts');

// 添加搜索建议（自动补全）
const suggestions = await fetch(`/api/suggest?q=${encodeURIComponent(query)}`);

// 搜索结果预览（上下文片段）
const highlightResult = hit._highlightResult;
const snippet = highlightResult.content.value.substring(0, 200) + '...';
```

---

### `Module 20` — Homepage Bottom Gallery Banner 首页底部画廊横幅
```javascript
// 功能：首页底部的图片展示墙，可链接到图集/相册
// 布局：横向滚动或瀑布流
```
| 模式 | 说明 |
|------|------|
| `scroll` | 横向拖拽滚动，无限循环 |
| `masonry` | 瀑布流，点击放大 |
| `carousel` | 自动轮播，指示器导航 |

**修改方式**：
```javascript
// 接入 Instagram/Flickr API 实时同步
const feed = await fetch(`https://graph.instagram.com/me/media?fields=...`);

// 添加点赞交互
galleryItem.addEventListener('dblclick', (e) =&gt; {
    createHeartAnimation(e.clientX, e.clientY);
    incrementLike(photoId);
});

// 懒加载 + 模糊渐进
const img = new Image();
img.src = thumbnailUrl;  // 先加载模糊小图
img.onload = () =&gt; {
    fullResImg.src = fullUrl;  // 再加载高清图
    fullResImg.classList.add('loaded');
};
```

---

### `Module 21` — Post Page Init 文章页初始化
```javascript
// 功能：文章页的模块组合调用
// 执行顺序：语法高亮 → 图片处理 → TOC生成 → 相关文章 → 评论加载
```

**修改方式**：
```javascript
// 添加阅读进度保存（跨设备同步）
const readingProgress = {
    url: location.href,
    scrollPercent: 0,
    timestamp: Date.now()
};
syncToServer(readingProgress);  // 或 localStorage

// 返回时恢复位置
window.addEventListener('load', () =&gt; {
    const saved = await getProgress(location.href);
    if (saved &amp;&amp; saved.scrollPercent &lt; 0.95) {
        window.scrollTo(0, saved.scrollPercent * document.body.scrollHeight);
        showResumePrompt(saved);
    }
});
```

---

### `Module 22` — Index Page Init 首页初始化
```javascript
// 功能：首页的模块组合调用
// 执行顺序：英雄区 → 卡片网格 → 分页/无限滚动 → 底部画廊
```

---

### `Module 23` — Main Entry 主入口
```javascript
// 功能：路由分发 + 模块调度器
// 根据页面类型（home/post/tag/archive）加载对应初始化模块
```

```javascript
// 路由映射表
const ROUTES = {
    '/': ['00', '01', '02', '03', '04', '06', '09', '10', '13', '14', '20', '23'],
    '/post/*': ['00', '01', '02', '03', '04', '06', '07', '08', '12', '15', '16', '17', '21', '23'],
    '/tag/*': ['00', '01', '02', '03', '04', '06', '11', '13', '23'],
    '/search': ['00', '01', '02', '03', '19', '23']
};
```

---

## 三、CSS 区域详解

### `Section 00` — Font Imports 字体导入
```css
/* 当前加载 */
@import url('https://fonts.googleapis.com/css2?family=Noto+Serif+SC:wght@400;700&amp;family=JetBrains+Mono&amp;display=swap');

/* 建议添加 */
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@300;400;500;700&amp;display=swap'); /* 无衬线正文 */
```

---

### `Section 01` — CSS Variables + Reset 变量与重置
```css
:root {
    /* 核心变量 */
    --font-serif: 'Noto Serif SC', serif;
    --font-sans: 'Noto Sans SC', sans-serif;
    --font-mono: 'JetBrains Mono', monospace;
    
    /* 动态主题变量（由 JS 切换） */
    --bg-primary: #faf7f5;
    --bg-secondary: #f0ebe7;
    --text-primary: #2c2420;
    --text-secondary: #6b5e55;
    --accent: #e8919c;        /* 主题色 */
    --accent-hover: #d67a85;  /* 悬停态 */
    --glass-bg: rgba(255,255,255,0.7);
    --glass-border: rgba(255,255,255,0.3);
    
    /* 间距系统 */
    --space-xs: 0.25rem;   /* 4px */
    --space-sm: 0.5rem;    /* 8px */
    --space-md: 1rem;      /* 16px */
    --space-lg: 1.5rem;    /* 24px */
    --space-xl: 2rem;      /* 32px */
    --space-2xl: 3rem;     /* 48px */
    
    /* 圆角系统 */
    --radius-sm: 4px;
    --radius-md: 8px;
    --radius-lg: 12px;
    --radius-xl: 16px;
    --radius-full: 9999px;
    
    /* 阴影系统 */
    --shadow-sm: 0 1px 2px rgba(0,0,0,0.05);
    --shadow-md: 0 4px 6px -1px rgba(0,0,0,0.1);
    --shadow-lg: 0 10px 15px -3px rgba(0,0,0,0.1);
    --shadow-glow: 0 0 20px var(--accent-alpha);
}
```

---

### `Section 02` — Welcome Splash Overlay 欢迎闪屏样式

| 类名 | 用途 |
|------|------|
| `.splash-overlay` | 全屏固定层 |
| `.splash-brand` | 品牌文字容器 |
| `.splash-progress` | 底部进度条轨道 |
| `.splash-progress-bar` | 进度填充动画 |

---

### `Section 03` — Background Image 背景图

```css
/* 主题背景映射 */
[data-theme='sakura'] {
    --bg-image: url('/assets/bg-sakura.jpg');
    --bg-overlay: linear-gradient(180deg, rgba(250,247,245,0.9) 0%, rgba(250,247,245,0.6) 100%);
}

[data-theme='ocean'] {
    --bg-image: url('/assets/bg-ocean.jpg');
    --bg-overlay: linear-gradient(180deg, rgba(240,248,255,0.9) 0%, rgba(240,248,255,0.6) 100%);
}
```

---

### `Section 04` — Typography 排版

| 模式 | 字体 | 适用场景 |
|------|------|----------|
| Day | `Noto Serif SC` | 正文阅读，传统优雅 |
| Night | `Noto Sans SC` | 暗色下更清晰 |
| 代码 | `JetBrains Mono` | 等宽，连字支持 |

---

### `Section 05` — Navbar 导航栏

**Liquid Glass 效果**：
```css
.navbar {
    background: var(--glass-bg);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
    border-bottom: 1px solid var(--glass-border);
}
```

---

### `Section 06-07` — Hero 区域

见 Module 09-10 说明。</description><guid isPermaLink="true">https://luliy.me/post/JS%20CSS%20-xiang-jie.html</guid><pubDate>Mon, 08 Jun 2026 14:14:40 +0000</pubDate></item><item><title>Luliy Blog v8 详细说明书</title><link>https://luliy.me/post/Luliy%20Blog%20v8%20-xiang-xi-shuo-ming-shu.html</link><description>## 📋 JavaScript 模块详解（enhance.js）

### 核心初始化模块
| 模块 | 功能 | 修改建议 | 增强思路 |
|------|------|--------|--------|
| **00 Welcome Splash** | 欢迎屏幕/加载动画 | 调整动画时长、背景颜色、文字内容 | 添加加载进度百分比显示 |
| **01 localStorage Init** | 本地存储初始化 | 配置存储键名、默认值 | 添加存储版本控制、过期时间 |
| **02 Progress Bar** | 页面滚动进度条 | 调整颜色、高度、位置 | 支持多段进度显示 |
| **03 Dynamic Title** | 动态页面标题 | 修改标题切换文案 | 添加表情符号动画 |

### 视觉交互模块
| 模块 | 功能 | 修改建议 | 增强思路 |
|------|------|--------|--------|
| **04 Uptime Counter** | 运行时间计数器 | 改起始日期、格式 | 添加服务器宕机检测、历史记录 |
| **05 Dark-mode Ripple** | 黑暗模式切换水波纹 | 调整波纹颜色、扩散速度 | 支持系统偏好自动切换 |
| **06 Static Background** | 静态背景（已移除粒子） | 更换背景图片URL | 添加时间段不同背景 |
| **07 Web Audio SFX** | 音效系统 | 替换音频文件、调整音量 | 添加音效开关、音量控制 |
| **08 Click Sparks** | 点击火花特效 | 调整粒子数量、颜色、速度 | 支持自定义形状、轨迹 |

### 页面结构模块
| 模块 | 功能 | 修改建议 | 增强思路 |
|------|------|--------|--------|
| **09 Hero Cluster** | 头部集群（头像+名字+时钟） | 修改头像、名字、时钟格式 | 添加社交媒体链接、动态问候 |
| **10 Hero Banner** | 主视觉横幅 | 更换背景、调整高度 | 支持滑动切换多个横幅 |
| **11 Tag Search Toolbar** | 标签页搜索栏 | 修改搜索框样式、占位符 | 添加搜索建议、热门标签显示 |
| **12 Image Lightbox** | 图片灯箱 | 调整动画、背景透明度 | 支持缩略图导航、图片预加载 |

### 交互工具模块
| 模块 | 功能 | 修改建议 | 增强思路 |
|------|------|--------|--------|
| **13 Floating Toolbar** | 浮动工具栏 + 统一沉底菜单（4主题） | 修改按钮数量、主题配色 | 添加工具栏自定义排序、主题预览 |
| **14 Home Card Rebuild** | 首页卡片重构 | 调整卡片大小、间距、阴影 | 支持卡片拖拽排序、多列布局 |
| **15 macOS Code Block** | macOS风格代码块 | 修改代码块颜色、字体 | 添加代码行号、复制按钮、语法高亮 |

### 动画特效模块
| 模块 | 功能 | 修改建议 | 增强思路 |
|------|------|--------|--------|
| **16 Sakura Petals** | 樱花飘落特效 | 调整花瓣数量、下落速度、季节 | 支持其他季节花朵、风力系统 |
| **17 ArticleTOC** | 文章目录+滚动监听 + 返顶按钮 | 修改TOC位置、返顶按钮样式 | 添加目录展开/折叠、平滑滚动 |

### 导航交互模块
| 模块 | 功能 | 修改建议 | 增强思路 |
|------|------|--------|--------|
| **18 Mobile Nav** | 手机导航 +汉堡菜单 + 下拉菜单 | 调整菜单项、动画时长 | 支持子菜单、手势导航 |
| **19 Search Overlay** | 搜索浮窗 | 修改搜索框样式、搜索范围 | 添加搜索历史、智能建议 |

### 页面初始化模块
| 模块 | 功能 | 修改建议 | 增强思路 |
|------|------|--------|--------|
| **20 Gallery Banner** | 首页底部画廊横幅 | 更换图片、调整排列 | 支持图片懒加载、缩放预览 |
| **21 Post Page Init** | 文章页面初始化 | 配置文章样式 | 添加阅读时间估算、分享功能 |
| **22 Index Page Init** | 索引页面初始化 | 配置列表样式 | 添加分页、排序筛选 |
| **23 Main Entry** | 主入口 | 管理模块加载顺序 | 添加模块延迟加载、条件加载 |

---

## 🎨 CSS 模块详解（enhance.css）

### 基础样式
| 部分 | 功能 | 修改建议 | 增强思路 |
|------|------|--------|--------|
| **00 Font Imports** | 字体导入 | 替换字体家族、添加本地字体 | 支持动态字体加载、字体预热 |
| **01 CSS Variables + Reset** | CSS变量 + 重置 | 修改主色调、间距单位 | 添加更多主题变量、响应式变量 |
| **02 Welcome Splash** | 欢迎屏样式 | 调整背景、文字颜色、覆盖层 | 支持动画序列、渐变背景 |
| **03 Background Image** | 背景图像 | 更换背景URL、调整位置 | 支持多层背景、视差效果 |

### 排版与主题
| 部分 | 功能 | 修改建议 | 增强思路 |
|------|------|--------|--------|
| **04 Typography** | 字体 / 日间=黑 / 夜间=白 | 调整字体大小、行高、颜色对比 | 添加高对比度模式、字体缩放 |
| **15 Article Theme** | 4种主题的CSS变量 | 修改主题配色方案 | 添加新主题、主题过渡动画 |

### 导航与布局
| 部分 | 功能 | 修改建议 | 增强思路 |
|------|------|--------|--------|
| **05 Navbar** | 液态玻璃 + 可折叠导航 | 调整透明度、高度、动画 | 支持导航吸顶、背景模糊 |
| **06 Hero Cluster** | 头部集群样式 | 调整头像大小、间距 | 支持动画入场、响应式布局 |
| **07 Hero Banner** | 主视觉横幅样式 | 修改高度、背景、文字位置 | 支持视差滚动、多层叠加 |

### 组件样式
| 部分 | 功能 | 修改建议 | 增强思路 |
|------|------|--------|--------|
| **08 Progress Bar** | 进度条样式 | 调整颜色、宽度、位置 | 支持渐变色、动画效果 |
| **09 Cards** | 首页卡片样式 | 修改边框、阴影、圆角 | 支持悬停动画、卡片分组 |
| **10 Date Badge** | 日期徽章样式 | 调整背景、文字、形状 | 支持相对时间显示 |
| **11 Pinned Section** | 置顶部分样式 | 修改置顶标题、背景 | 支持多级置顶、固定区域 |

### 内容展示
| 部分 | 功能 | 修改建议 | 增强思路 |
|------|------|--------|--------|
| **12 Article Reading** | 文章阅读面板（主题特定） | 调整宽度、行高、背景 | 支持字体大小调节、无衬线切换 |
| **13 Code Blocks** | 代码块（日间+暗夜） | 修改语法高亮、字体、边框 | 支持代码行号、复制提示 |
| **14 Tables** | 表格（日间+暗夜） | 调整表格边框、背景、文字 | 支持表格排序、固定表头 |

### 功能性样式
| 部分 | 功能 | 修改建议 | 增强思路 |
|------|------|--------|--------|
| **16 TOC Scroll-spy** | 目录滚动监听 + 活跃高亮 | 修改高亮颜色、动画 | 支持目录折叠、平滑指示 |
| **17 Floating Toolbar** | 浮动工具栏 + 沉底菜单 | 调整按钮样式、位置、大小 | 支持工具栏收缩、快捷键 |
| **18 Prev/Next** | 前后导航 | 修改按钮文案、样式 | 支持缩略图预览、键盘导航 |
| **19 Lightbox** | 灯箱样式 | 调整背景、过渡动画 | 支持图片信息显示、缩放控制 |
| **20 Back-to-Top** | 返顶按钮 | 修改按钮样式、位置 | 支持滚动高度阈值、快捷键 |

### 特殊效果
| 部分 | 功能 | 修改建议 | 增强思路 |
|------|------|--------|--------|
| **21 Sakura Canvas** | 樱花画布 / 主题波纹 | 调整花瓣颜色、密度 | 支持季节切换、自定义粒子 |
| **22 Uptime** | 正常运行时间样式 | 修改字体、颜色、格式 | 支持历史记录展示、图表 |
| **23 Timeline/Archive** | 时间轴/归档 | 调整时间轴样式、颜色 | 支持按年/月分组、搜索 |
| **24 Gallery Banner** | 画廊横幅 | 修改图片排列、间距 | 支持图片网格、灯箱集成 |
| **25 Search Overlay** | 搜索浮窗 | 调整搜索框样式、背景 | 支持结果预览、搜索历史 |
| **26 Responsive** | 响应式（手机） | 调整断点、间距、字体 | 支持平板/大屏优化、触摸优化 |

---

## 📄 JSON 配置文件说明

### 常见配置文件结构

```json
{
  'site': {
    'title': 'Luliy Blog',
    'description': '个人博客',
    'author': 'Your Name',
    'url': 'https://your-domain.com'
  },
  'theme': {
    'primaryColor': '#6366f1',
    'darkMode': true,
    'themes': ['light', 'dark', 'sakura', 'midnight']
  },
  'modules': {
    'particles': false,
    'audioSFX': true,
    'sakuraPetals': true,
    'darkModeRipple': true
  },
  'navbar': {
    'sticky': true,
    'glassEffect': true,
    'transparent': false
  },
  'timeline': {
    'enabled': true,
    'groupBy': 'year'
  }
}
```

### 关键配置项修改指南

| 配置项 | 类型 | 说明 | 修改例|
|--------|------|------|--------|
| `primaryColor` | String | 主色调 | `'#ff6b6b'` |
| `theme` | Array | 启用主题列表 | `['light', 'dark']` |
| `audioSFX` | Boolean | 启用音效 | `false` |
| `sakuraPetals` | Boolean | 樱花效果 | `true` |
| `uptimeStart` | String | 正常运行起始日期 | `'2024-01-01'` |

---

## 💡 功能增强建议

### 建议新增模块
1. **24Comment System** - 评论系统 / Giscus 集成
2. **25 Statistics Dashboard** - 阅读统计面板
3. **26 Reading Mode** - 专注阅读模式（无干扰）
4. **27 Export Articles** - 导出文章为PDF/Markdown
5. **28 Dynamic Weather** - 显示实时天气
6. **29 Notification Center** - 消息/评论通知

### 建议新增CSS主题
1. 高对比度模式（无障碍）
2. 护眼模式（暖色调）
3. 自动主题（跟随系统）
4. 季节主题（春/夏/秋/冬）

---

需要我详细讲解某个具体模块或帮你修改特定功能吗？。</description><guid isPermaLink="true">https://luliy.me/post/Luliy%20Blog%20v8%20-xiang-xi-shuo-ming-shu.html</guid><pubDate>Mon, 08 Jun 2026 14:07:56 +0000</pubDate></item><item><title>gmeek ui</title><link>https://luliy.me/post/gmeek%20ui.html</link><description># 🍎 Gmeek UI 增强插件使用教程

## 📦 插件简介

Gmeek UI 插件通过纯前端增强的方式，为 Gmeek 框架博客带来 Apple 风格的焕然一新体验，包括：

| 文件名                   | 功能说明                                                                 |
|--------------------------|--------------------------------------------------------------------------|
| `GmeekBaseTheme.css`     | 基础布局样式：字体、圆角、卡片排版，兼容原始结构不污染构建样式                          |
| `GmeekCustomizeCss.js`   | UI 动态增强：卡片结构重构、玻璃拟态样式、深浅模式自适应、渐变背景、标签配色自动对比等功能       |

&gt; 🧠 零依赖、构建无侵入、插拔可选、明暗自动识别、视觉沉浸体验，100% 兼容 Gmeek 原生内容结构。</description><guid isPermaLink="true">https://luliy.me/post/gmeek%20ui.html</guid><pubDate>Thu, 04 Jun 2026 16:33:48 +0000</pubDate></item><item><title>在线 Markdown 编辑器</title><link>https://luliy.me/post/zai-xian-%20Markdown%20-bian-ji-qi.html</link><description>## 在线 Markdown 编辑器
- 地址：[点击前往](https://markdown.buxiantang.top/)。</description><guid isPermaLink="true">https://luliy.me/post/zai-xian-%20Markdown%20-bian-ji-qi.html</guid><pubDate>Thu, 04 Jun 2026 15:54:39 +0000</pubDate></item><item><title>看病</title><link>https://luliy.me/post/kan-bing.html</link><description>## 1.外地的病人，最好先在当地三甲做完该做的检查，拿着报告来北京找大夫看，要不然到北京大医院检查排队时间可就没准了。</description><guid isPermaLink="true">https://luliy.me/post/kan-bing.html</guid><pubDate>Wed, 03 Jun 2026 15:20:22 +0000</pubDate></item><item><title>friends</title><link>https://luliy.me/post/friends.html</link><description># 学习

&lt;details&gt;
&lt;summary&gt;CET pro&lt;/summary&gt;


&gt; [CET4](http://luliy6.42web.io/)

&gt;[md官方指南](https://docs.github.com/zh/get-started/writing-on-github/getting-started-with-writing-and-formatting-on-github/basic-writing-and-formatting-syntax)


&lt;/details&gt;

# **VIBE CODING**
[李厚辰主题讲](http://youtube.com/watch?v=1TqrucFn4qY)

# `学习网站`

&lt;details&gt;
&lt;summary&gt;学习&lt;/summary&gt;

1. [Bro code](https://www.youtube.com/)
如果您想学习编程，这个频道适合您。</description><guid isPermaLink="true">https://luliy.me/post/friends.html</guid><pubDate>Wed, 03 Jun 2026 13:54:14 +0000</pubDate></item><item><title>money</title><link>https://luliy.me/post/money.html</link><description>![image](https://github.com/user-attachments/assets/ac73014b-6f0a-42f7-873c-cb8420e0da4a)。</description><guid isPermaLink="true">https://luliy.me/post/money.html</guid><pubDate>Wed, 03 Jun 2026 13:39:12 +0000</pubDate></item><item><title>本站指南</title><link>https://luliy.me/post/ben-zhan-zhi-nan.html</link><description># Luliy Blog v6 增强功能完整说明书

本文档基于您提供的 `enhance.js`、`enhance.css` 以及 Gmeek 配置文件 `config.json` 编写。</description><guid isPermaLink="true">https://luliy.me/post/ben-zhan-zhi-nan.html</guid><pubDate>Tue, 02 Jun 2026 13:37:56 +0000</pubDate></item><item><title>Music</title><link>https://luliy.me/post/Music.html</link><guid isPermaLink="true">https://luliy.me/post/Music.html</guid><pubDate>Mon, 01 Jun 2026 17:53:07 +0000</pubDate></item><item><title>养生</title><link>https://luliy.me/post/yang-sheng.html</link><description>&gt; 补充一篇 24 年初的文章，分享最近一年的一些个人实践：[养生博主的 23 年总结](https://zhuanlan.zhihu.com/p/675470252)。</description><guid isPermaLink="true">https://luliy.me/post/yang-sheng.html</guid><pubDate>Sat, 30 May 2026 13:58:09 +0000</pubDate></item><item><title>prompt</title><link>https://luliy.me/post/prompt.html</link><description># 英语
## CET4
&lt;details&gt;
&lt;summary&gt;CET pro&lt;/summary&gt;

```
 Role: The Syntax Deep Dive Master
## Profile &amp; Objective
You are a Senior English Teacher and Language Analyst with over 20 years of experience. Your pedagogical approach focuses on patience, systematic breakdown, and structural logic.
**Your Goal:** To deconstruct complex English syntactic structures (S-V-O, clauses, modification chains) for beginners/intermediate learners. You do not just translate; you help the student **internalize the logic** of the language.
## Core Execution Framework
The user will provide learning material (a single sentence or a full article/paragraph). You must **automatically and strictly** execute one of the following two modes based on the input.
### Mode 1: Single Sentence Deep Dive
**Trigger:** User inputs a single sentence.
**Execution:** You must follow these phases sequentially.
#### Phase 1: Morphological Segmentation (Table Driven)
1. 	**Deconstruct:** Break the sentence down into its smallest meaningful units (morphemes, words, or key phrases).
2. 	**Format:** Present these units in a **Markdown Table**.
3. 	**Pre-requisite Check:** After displaying the table, you must immediately ask: '请您确认，表格中的所有词素/短语您是否都已完全理解？请回复‘是’或‘否’，以便我们继续。</description><guid isPermaLink="true">https://luliy.me/post/prompt.html</guid><pubDate>Sat, 30 May 2026 10:45:20 +0000</pubDate></item><item><title>&lt;!-- ##{"timestamp":1743429389}## --&gt;</title><link>https://luliy.me/post/-%21--%20%23%23%7B-timestamp--1743429389%7D%23%23%20---.html</link><description>&lt;!-- ##{'timestamp':1743429389}## --&gt;

# 0. 简介
&gt; [Gmeek](https://github.com/Meekdai/Gmeek) 一个博客框架，超轻量级个人博客模板，完全基于Github Pages 、 Github Issues 和 Github Actions，可以称作All in Github。</description><guid isPermaLink="true">https://luliy.me/post/-%21--%20%23%23%7B-timestamp--1743429389%7D%23%23%20---.html</guid><pubDate>Mon, 31 Mar 2025 13:56:29 +0000</pubDate></item><item><title>about</title><link>https://luliy.me/about.html</link><description># 关于我
Luliy，
23岁，银川能源学院，机械专业。</description><guid isPermaLink="true">https://luliy.me/about.html</guid><pubDate>Sat, 30 May 2026 10:29:29 +0000</pubDate></item><item><title>book</title><link>https://luliy.me/book.html</link><description>哈喽。</description><guid isPermaLink="true">https://luliy.me/book.html</guid><pubDate>Sat, 30 May 2026 10:43:19 +0000</pubDate></item><item><title>gallery</title><link>https://luliy.me/gallery.html</link><description>&lt;img width='1672' height='941' alt='Image' src='https://github.com/user-attachments/assets/4a6cb22c-6459-4605-9605-3daa7b7da42b' /&gt;
&lt;img width='3344' height='1882' alt='Image' src='https://github.com/user-attachments/assets/7ecae118-93d7-4c83-9a6c-2973535bf479' /&gt;
&lt;img width='1672' height='941' alt='Image' src='https://github.com/user-attachments/assets/c7f210ae-89e9-4912-82b5-40d409dbf4f4' /&gt;
&lt;img width='1672' height='941' alt='Image' src='https://github.com/user-attachments/assets/6d1940a4-e3c9-44ea-af27-a58f8a9d015c' /&gt;
&lt;img width='1672' height='941' alt='Image' src='https://github.com/user-attachments/assets/7632475b-8b65-4643-b386-6526650559a0' /&gt;
&lt;img width='1672' height='941' alt='Image' src='https://github.com/user-attachments/assets/3872b636-5649-4653-8f85-da035c9f0024' /&gt;
&lt;img width='3344' height='1882' alt='Image' src='https://github.com/user-attachments/assets/f8995d38-20f4-4da4-9706-b427f214137e' /&gt;。</description><guid isPermaLink="true">https://luliy.me/gallery.html</guid><pubDate>Mon, 01 Jun 2026 17:52:29 +0000</pubDate></item><item><title>favorites</title><link>https://luliy.me/favorites.html</link><description># 日记
## 还是来了 20260601
我发现一个非常重要的问题，
我的大计划是学习英语四级，
小计划是个人成长，却一项都没有落实。</description><guid isPermaLink="true">https://luliy.me/favorites.html</guid><pubDate>Tue, 02 Jun 2026 16:25:37 +0000</pubDate></item></channel></rss>